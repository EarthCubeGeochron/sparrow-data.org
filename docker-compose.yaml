version: "3"
services:
  gateway:
    build: .
    restart: unless-stopped
    ports:
      - 8080:80/tcp
      - 443:443/tcp
    volumes:
      - ./htdocs:/www/data
    environment:
      - CERTBOT_EMAIL=daven.quinn@wisc.edu
